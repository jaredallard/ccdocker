 --[[
  Make a docker image!

  Author: Jared Allard <rainbowdashdc@pony.so>
  License: MIT
  Version: 0.0.1
]]

local docker = loadfile("/docker/docker.lua")()
local json = loadfile("/docker/json.lua")()
local Args = {...}
local directory = Args[1]

-- verify it exists
if fs.exists(directory) == false then
  error("directory not found")
end

docker:makeImage(directory)
